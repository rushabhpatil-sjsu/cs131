A1: ls -l > listing.txt && date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt | file descriptor 2 = stderr
A3: ls /nonexistent_dir /etc 1> output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: ls /nonexistent_dir /etc 2>/dev/null | only /etc listing shown, error discarded. /dev/null is a black hole device file that discards anything written to it
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep -c 'ERROR' server.log
A9: grep 'ERROR' server.log > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -nr
A11: grep -oE '"state":"[A-Z]+"' /data/yelp/yelp_academic_dataset_business.json | sort | uniq -c | sort -nr | head -5
B1: id | uid=1066, gid=1066, groups=ishigaki-cs33,video,docker,render. uid identifies an individual user, gid identifies a group permission can be shared by multiple users
B2: ls -l fruits.txt | -rw-rw-r-- : owner=read+write, group=read+write, other=read-only
B3: umask | 0002, predicts new files get rw-rw-r-- (666 minus 002), verified with touch testfile.txt and ls -l
B4: chmod 000 secret.txt (cat gave Permission denied) then chmod u+rw secret.txt to restore owner read+write
B5: chmod 640 fruits.txt | octal 640 = owner rw, group r, other none
B6: find ~/cs131/ws3 -type f -user ishigaki-cs33 -perm -u+w
C1: bash long.sh > log.txt & | PID 925866, confirmed accumulating output with tail log.txt
C2: jobs | shows job number [1], status Running, and the command bash long.sh > log.txt
C3: fg then Ctrl-Z to suspend | jobs now shows status Stopped
C4: bg | confirmed via jobs that status is Running again
C5: kill %1 | confirmed Terminated via jobs. Default signal sent by kill is SIGTERM (signal 15)
C6: bash long.sh > log.txt & then disown %1 | PID 926337, confirmed not in jobs but still running via ps -p 926337
C7: disown removes the process from the shell's job table so it no longer receives the SIGHUP signal sent when the terminal closes, while a regular background job is still tied to the shell and gets killed by SIGHUP on exit.
